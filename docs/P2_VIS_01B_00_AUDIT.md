# P2_VIS_01B_00_AUDIT.md

Date: 2026-06-23

Scope: pre-implementation audit and design for the real city road-network demo.
This VIS-01B-00 pass did not install packages, change system settings, download
OSM data, run city solvers, or generate Folium demo files.

Note: the current working tree already contains some VIS-01B-01 implementation
artifacts from an earlier run. They are audited as existing state here, but were
not generated by this VIS-01B-00 pass.

## A. Quality Gates

Commands were run from `<repo-root>` with `.venv311` activated.

| Check | Result |
| --- | --- |
| Python | `Python 3.11.9` |
| Python executable | `<repo-root>\.venv311\Scripts\python.exe` |
| `python -m pip check` | Pass: no broken requirements |
| `python -m pytest -q` | Pass: 185 passed, 1 skipped, 3 warnings |
| `python -m ruff check .` | Pass |
| `python -m mypy src` | Pass: 44 source files |
| `vrptw info` | Pass |
| Geo/app dependency smoke | Pass: `osmnx`, `networkx`, `folium`, `streamlit` import |

## B. Current OSM / City / Synthetic / GeoJSON / Folium / Streamlit Capabilities

| Module | Current capability | Evidence |
| --- | --- | --- |
| `src/vrptw_hybrid/data/osm_network.py` | GraphML load/save, OSMnx download wrapper, nearest node lookup, network shortest-path distance/time matrices, shortest-path geometry helpers | `download_graph`, `load_graphml`, `save_graphml`, `nearest_nodes_for_orders`, `network_distance_time_matrix`, `build_shortest_path_geometry` |
| `src/vrptw_hybrid/data/synthetic.py` | Reproducible synthetic city VRPTW instances from graph nodes with lat/lon, demand, service times, and time windows | `CityGenerationConfig`, `generate_city_vrptw_instance` |
| `src/vrptw_hybrid/data/city_instance.py` | City artifact save/load and conversion from generated instance to network-matrix city instance | `save_city_instance_json`, `load_city_instance_json`, `with_network_matrices` |
| `src/vrptw_hybrid/data/distance_matrix.py` | Euclidean/rounding/scaling helpers | Existing helper module |
| `src/vrptw_hybrid/visualization/geojson.py` | Point and route GeoJSON generation; routes can use graph shortest paths when graph node ids are present | `solution_geojson`, `routes_feature_collection` |
| `src/vrptw_hybrid/visualization/folium_map.py` | Folium HTML rendering with depot/customer markers, route polylines, optional vehicle layers and caption | `save_solution_map_html`, `render_solution_map` |
| `apps/streamlit_app.py` | Benchmark viewer remains separate; current working tree also has a `City road demo` mode for curated city artifacts | Sidebar modes include benchmark and city demo |
| `src/vrptw_hybrid/cli.py` | Existing CLI supports info/solve/batch/benchmark plot; no separate city subcommand is required for VIS-01B-00 | Existing CLI |

Important separation:

- VIS-01A benchmark x-y viewer is not a real map and must stay labeled as
  benchmark coordinates.
- VIS-01B city road demo must use real lat/lon and OSM road-network geometry.
- CP-SAT is not part of the city demo solver set.

## C. Existing GraphML / Cache State

Current local cache audit:

| Path | Status | Size | Commit policy |
| --- | --- | ---: | --- |
| `cache/osm/berlin_mitte_drive.graphml` | Exists | 1,101,744 bytes | Must stay ignored |
| `cache/distance_matrices/berlin_mitte_30_seed42.npz` | Exists | 15,914 bytes | Must stay ignored |
| `data/raw/osm` | Missing | n/a | Must stay ignored if created |
| `reports/demo/city/` | Exists with curated city artifacts from earlier run | small files | Curated demo artifacts may be committed |

This VIS-01B-00 pass did not download or regenerate these files.

## D. Berlin Mitte 30-Order Demo Design

Recommended minimum city demo:

| Field | Design |
| --- | --- |
| `city_id` | `berlin_mitte_30` |
| `place_name` | `Mitte, Berlin, Germany` |
| network type | `drive` |
| bbox | north `52.5320`, south `52.5100`, east `13.4200`, west `13.3700` |
| order count | 30 |
| seed | 42 |
| vehicle count | 6 |
| vehicle capacity | 35 |
| demand range | 1 to 4 |
| service time | 10 minutes |
| time horizon | 720 minutes |
| solvers | `greedy`, `ortools_routing`, `alns_roulette`, `alns_mosade` |
| excluded solver | `cp_sat` |
| time limits | 30 seconds for OR-Tools Routing and ALNS |

Distance/time design:

- Distance matrix: OSM road-network shortest-path distance in meters.
- Time matrix: proxy minutes derived from OSM edge length and simple speed
  assumptions.
- Do not claim measured travel-time data or live travel times.

## E. OSM Download Plan For VIS-01B-01

If the existing cache is reused, no OSM download is needed in this workspace.
If the cache is absent or regeneration is requested, ask the user for explicit
confirmation before downloading.

Planned download:

- Area label: `Mitte, Berlin, Germany`
- Spatial query: small Berlin Mitte bbox
  - north `52.5320`
  - south `52.5100`
  - east `13.4200`
  - west `13.3700`
- `network_type`: `drive`
- GraphML cache path: `cache/osm/berlin_mitte_drive.graphml`
- Distance matrix cache path:
  `cache/distance_matrices/berlin_mitte_30_seed42.npz`
- Reuse policy: load GraphML if present; download only if missing and confirmed.
- Failure fallback: use a smaller bbox or ask the user to provide GraphML at the
  cache path.

## F. Needed Files For VIS-01B-01

In a clean VIS-01B-01 implementation, expected additions/modifications are:

| File | Purpose | Current status in this workspace |
| --- | --- | --- |
| `configs/city_demo_berlin_mitte.yaml` | City demo config | Exists |
| `src/vrptw_hybrid/data/osm_network.py` | OSM download/cache/matrix/geometry helpers | Exists with city helpers |
| `src/vrptw_hybrid/data/city_instance.py` | City instance artifact save/load and network-matrix instance conversion | Exists |
| `scripts/generate_city_demo.py` | Orchestrates GraphML, instance generation, solve, GeoJSON, Folium outputs | Exists |
| `src/vrptw_hybrid/visualization/geojson.py` | City route GeoJSON fields | Exists |
| `src/vrptw_hybrid/visualization/folium_map.py` | Folium map rendering for real lat/lon routes | Exists |
| `apps/streamlit_app.py` | Separate city road demo mode | Exists |
| `tests/test_city_demo.py` | Offline city pipeline tests with mock graph | Exists |
| `docs/P2_VIS_01B_REPORT.md` | VIS-01B implementation report | Exists from earlier run |

VIS-01B-00 itself only requires this audit/design report.

## G. Git / Artifact Policy

Must stay ignored:

- `.venv311/`
- `.ai-bridge/`
- `cache/`
- `cache/osm/`
- `cache/distance_matrices/`
- `data/raw/`
- `data/results/`

May be committed:

- `docs/P2_VIS_01B_00_AUDIT.md`
- city demo config/source/tests/docs when VIS-01B-01 is intentionally included
- small curated files under `reports/demo/city/`

Do not commit GraphML, distance-matrix cache, raw OSM data, or experiment
scratch outputs.

## H. Readiness

Can enter VIS-01B-01: Yes, from a design and dependency perspective.

Does VIS-01B-01 need OSM download confirmation:

- Current workspace: no, if reusing existing `cache/osm/berlin_mitte_drive.graphml`.
- Clean workspace or cache regeneration: yes, explicit confirmation is required
  before any OSMnx/Overpass download.

VIS-01B-01 should not use CP-SAT as the city demo main solver and should keep
benchmark x-y viewer output separate from real city road-network output.
